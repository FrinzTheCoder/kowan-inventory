runtime: nodejs22
instance_class: F2

env_variables:
  NODE_ENV: "production"

# Optional: static files (only if using `npm run export`)
handlers:
- url: /
  static_files: out/index.html
  upload: out/index.html
- url: /(.*)
  static_files: out/\1
  upload: out/(.*)
